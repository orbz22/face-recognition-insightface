# Face Recognition System - InsightFace
# Requirements file untuk instalasi dependencies
# Compatible with Python 3.9 - 3.12

# Core dependencies
numpy>=1.26.0,<2.0.0  # Pin to <2.0 for compatibility
opencv-python>=4.8.0,<5.0.0
insightface>=0.7.3

# ONNX Runtime (pilih salah satu)
# Untuk CPU only (Raspberry Pi / PC tanpa GPU):
onnxruntime>=1.16.0

# Untuk GPU (uncomment jika punya NVIDIA GPU):
# onnxruntime-gpu>=1.16.0

# QR Code support (optional - may have DLL issues on some systems)
qrcode[pil]>=7.4.0
pyzbar>=0.1.9
cryptography>=41.0.0
pillow>=10.0.0

# Additional dependencies (otomatis terinstall dengan insightface)
# albumentations
# scipy
# scikit-image
# tqdm
# requests

# Performance monitoring
psutil>=5.9.0  # For CPU and memory monitoring
